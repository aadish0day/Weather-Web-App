version: '3.8'

services:
  weather-app:
    build:
      context: .
      args:
        VITE_WEATHER_API_KEY: ${VITE_WEATHER_API_KEY}
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
